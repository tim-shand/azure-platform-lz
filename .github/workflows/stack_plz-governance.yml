name: "Deploy Stack: PLZ-Governance"

on:
    workflow_dispatch:
        inputs:
            action:
                description: "Action: Plan, Apply, Destroy"
                required: true
                default: plan
                type: choice
                options:
                    - plan
                    - apply
                    - destroy
    pull_request:
        paths:
            - ".github/workflows/stack_plz_governance.yml"
            - "variables/plz-governance.tfvars"
            - "deployments/plz-governance/**"
        branches:
            - main

permissions:
    id-token: write
    contents: read

jobs:
    # PLAN ------------------------------------------------------- #
    plan:
        uses: ./.github/workflows/terraform-plan.yml
        with:
            # Inputs passed to template job.
            stack_name: plz-governance
            working_directory: ./deployments/plz-governance
            run_mode: ${{ inputs.action }}
        secrets:
            # Secrets must be declared separately from inputs and passed explicitly.
            ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}

    # APPLY / DESTROY -------------------------------------------- #
    apply:
        needs: plan # REQUIRES the plan job to succeed before running.
        if: github.event.inputs.action == 'apply' || github.event.inputs.action ==
            'destroy'
        uses: ./.github/workflows/terraform-apply.yml
        with:
            # Inputs passed to template job.
            stack_name: plz-governance
            working_directory: ./deployments/plz-governance
            run_mode: ${{ inputs.action }}
        secrets:
            # Secrets must be declared separately from inputs and passed explicitly.
            ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
