# Ignore
.notes/
.notes.md
.token
.config
*.log
*.test/
*.dev/

# Environments
.env
*.env
*.env.ps1
*env.psd1
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
# Allowed
!env-example.psd1

# Terraform directories, CLI config and state files.
**/.terraform/*
.terraform
.terraformrc
terraform.rc
tfplan.*
*.tfplan
*.plan
*.tfstate
*.tfstate.*
*.lock.*
crash.log
crash.*.log
backend.conf

# Exclude prod .tfvars files, which are likely to contain sensitive data, such as password, private keys, and other secrets.
*.tfvars
*.tfvars.json
# Allow example or default TFVARS file.
!example.tfvars
!terraform.tfvars

# Ignore override files as they are usually used to override resources locally.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Packer Files
*packer_cache/
